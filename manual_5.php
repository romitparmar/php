<?php
// function minNotes($amount) {
    // $notes = [100, 50, 20, 10, 5, 2, 1];

    // foreach ($notes as $note) {
        // if ($amount >= $note) {
            // $count = intdiv($amount, $note);
            // echo "Rs. $note x $count"."<br>";
            // $amount -= $count * $note;
        // }
    // }
// }

// Example
// minNotes(289);



// function minNote($amount){
//     $note = array(100, 50, 20, 10, 5, 2, 1);

//     foreach($note as $notes){
//         if($amount>=$notes){
//             $count = intdiv($amount,$notes);
//             echo "Rs".$notes."x".$count."<br>";
//             $amount-=$count*$notes;
//         }
//     }
// }
// minNote(289);


$str = "romit";

if ($str == strtolower($str)) {
    echo "string iss in lowercase!!!<br>";
} else {
    echo "string in uppercae!!!";
}
?>
